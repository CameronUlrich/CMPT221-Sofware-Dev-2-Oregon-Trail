document.body.addEventListener("keydown", function (event) {
		    if (event.keyCode === 32) {
		    	if(currentScreen == 4){
		        window.location.replace("/trail");
		        
		    }
		}
		});
